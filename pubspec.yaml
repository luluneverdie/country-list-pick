name: country_list_pick_with_nation
description: Flutter plugin project to pick country code, will show code country, dial code, flag country, and name country.
version: 0.0.1
homepage: https://github.com/luluneverdie/country-list-pick

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - flags/
